# wkhtmltopdf（已弃用）

巡检报告 PDF **已改为浏览器 html2canvas + jsPDF 导出**，后端镜像不再安装 wkhtmltopdf / Chromium。

本目录历史离线包可删除；保留 README 仅作迁移说明。

相关环境变量 `INSPECT_SERVER_PDF` / `INSPECT_USE_WKHTMLTOPDF` / `INSPECT_CHROMIUM_BIN` 已无效果。
