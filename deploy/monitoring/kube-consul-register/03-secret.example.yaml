# 拷为 secret.yaml 后填 Token；不要把真实 Secret 提交进 git。
#   echo -n '<metrics-register SecretID>' | base64
apiVersion: v1
kind: Secret
metadata:
  name: consul-token
  namespace: yunshu-monitor
type: Opaque
stringData:
  consul_token: "REPLACE_WITH_METRICS_REGISTER_TOKEN"
