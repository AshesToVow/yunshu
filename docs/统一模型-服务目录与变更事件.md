# 统一模型：服务目录与变更事件

Phase 1 地基 + Phase 2 产品闭环 + Phase 2.5 治理加深说明。

## Phase 1：三件套

| 能力 | 作用 |
|------|------|
| **插件契约（Manifest）** | 后端 `plugin.Manifest` + `GET /api/v1/plugins` 为菜单路径 / API 前缀的单一事实源 |
| **服务目录（service_catalog）** | 项目级稳定 `service_id`；`service_links` 关联各域实体 |
| **变更事件（change_events）** | 写操作汇入统一时间线 |

## Phase 2：产品闭环

| 能力 | 入口 | 说明 |
|------|------|------|
| **服务画像** | `/service-portrait` | 聚合 links、入口、最近变更、CI/CD 摘要、**健康分** |
| **变更中心** | `/change-center` | 日历 + 维护窗 + **冻结窗 CRUD** + 冲突/拦截提示 |
| **故障工作台** | `/incident-workbench` | P1/P2 告警 + **Incident 开单/时间线/MTTA-MTTR** |
| **K8s 治理** | preview-apply / snapshots | dry-run 文本 diff、impact、变更前快照回滚 |
| **发布后验证** | CI/CD 详情 Tab | Ready + 日志弱信号 + 新告警，写回 `verify_status` |

## Phase 2.5：治理加深

### 变更冻结与冲突

- 表：`change_freeze_windows`
- 门禁：`internal/service/changegate`（CI/CD execute、dbmgmt SQL 执行、K8s apply/delete）
- API：`/change-freeze-windows`、`/change-events/conflict-check`（同服务/namespace 15 分钟冲突提示）

### K8s 保护

- `POST /deployments/preview-apply`（StatefulSet 同路径）
- `GET /deployments/snapshots`、`POST /deployments/snapshots/rollback`
- Apply/Delete 前快照写入 `k8s_workload_snapshots`

### 发布验证

- `POST /projects/:id/cicd/release-runs/:runId/verify`
- 字段：`cicd_release_runs.verify_status` / `verify_json` / `verified_at`

### Incident

- 表：`incidents` / `incident_notes`
- API：`/incidents` CRUD 状态机 + `/timeline`

### 告警质量

- `GET /api/v1/alerts/quality-report`
- 菜单：`/alert-quality`

### 关键 API 摘要

```
GET/POST/DELETE /api/v1/projects/:id/change-freeze-windows
GET /api/v1/projects/:id/change-events/conflict-check
GET/POST /api/v1/projects/:id/incidents
PUT /api/v1/projects/:id/incidents/:incidentId
GET /api/v1/projects/:id/incidents/:incidentId/timeline
POST /api/v1/projects/:id/cicd/release-runs/:runId/verify
POST /api/v1/deployments/preview-apply
GET  /api/v1/deployments/snapshots
POST /api/v1/deployments/snapshots/rollback
GET  /api/v1/alerts/quality-report
```

## 数据关系

```
project
  ├── service_catalog → service_links
  ├── change_events / change_freeze_windows
  ├── incidents → incident_notes
  └── (via owning_project) k8s_workload_snapshots
```

## 写入约定

- CI/CD 服务 upsert → 自动目录 + `cicd_service` link
- 变更埋点：CI/CD release；K8s apply/scale/restart/delete/rollback；dbmgmt SQL；alert 维护窗
- 冻结拦截会写 `action=freeze_blocked` 的 aborted change_event

## 后续（Phase 3 其余专题）

SLO / 错误预算、成本容量、dbmgmt 脱敏、JIT 权限等。
